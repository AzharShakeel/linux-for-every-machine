# Networking and SSH Cheatsheet

Essential network and SSH commands for troubleshooting connectivity, checking open services, and moving files securely between systems.

```bash
ip a
ip r
ping -c 4 8.8.8.8
ss -tulpen
ssh user@server-ip
scp file.txt user@server-ip:/tmp/
```
